[package]
name = "shapes"
version = "0.1.0"
edition = "2021"

[dependencies]
noise = "0.7.0"
rand = "0.8.5"
rand_chacha = "0.3.1"
palette = { path = "../palette" }
transforms = { path = "../transforms" }
